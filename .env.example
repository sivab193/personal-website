# Optional: a GitHub token (classic, no scopes needed for public repos, or a
# fine-grained token with public read) lifts the API rate limit used to
# auto-sync project cards from 60/hr to 5000/hr. Set this in Vercel too.
GITHUB_TOKEN=
